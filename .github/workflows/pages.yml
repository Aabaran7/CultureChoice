name: Deploy to GitHub Pages

on:
  push:
<<<<<<< HEAD
    branches: [ main ]
=======
    branches: [main]
>>>>>>> b9b5639 (Mobile layout + Switch v-model; responsive slider)
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Checkout repo into smile/
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          path: smile

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: smile
        run: npm ci

      - name: Generate git environment (optional)
        working-directory: smile
        run: |
          if [ -x scripts/generate_git_env.sh ]; then
            chmod +x scripts/generate_git_env.sh
            sh scripts/generate_git_env.sh || true
          fi

      - name: Build
        working-directory: smile
=======
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate git environment
        run: |
          chmod +x scripts/generate_git_env.sh
          sh scripts/generate_git_env.sh

      - name: Install deps
        run: npm ci

      - name: Build
>>>>>>> b9b5639 (Mobile layout + Switch v-model; responsive slider)
        env:
          VITE_DEPLOY_BASE_PATH: /CultureChoice/
          VITE_FIREBASE_APIKEY: ${{ fromJson(secrets.FIREBASE_CONFIG_JSON).apiKey }}
          VITE_FIREBASE_AUTHDOMAIN: ${{ fromJson(secrets.FIREBASE_CONFIG_JSON).authDomain }}
          VITE_FIREBASE_PROJECTID: ${{ fromJson(secrets.FIREBASE_CONFIG_JSON).projectId }}
          VITE_FIREBASE_STORAGEBUCKET: ${{ fromJson(secrets.FIREBASE_CONFIG_JSON).storageBucket }}
          VITE_FIREBASE_MESSAGINGSENDERID: ${{ fromJson(secrets.FIREBASE_CONFIG_JSON).messagingSenderId }}
          VITE_FIREBASE_APPID: ${{ fromJson(secrets.FIREBASE_CONFIG_JSON).appId }}
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
<<<<<<< HEAD
          path: smile/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

 
=======
          path: dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
>>>>>>> b9b5639 (Mobile layout + Switch v-model; responsive slider)
